import 'dart:ui';

/// Application settings.
class AppSettings {
  /// the application name.
  static const String appName = 'MSplash Software';

  /// the application version.
  static const String appVersion = 'V.1.0';

  /// the application splash screen icon json.
  static const String splashIcon = 'assets/lottie/splash_icon.json';

  /// the application splash screen background color.
  static const Color splashBackgroundColor = Color.fromRGBO(27, 26, 23, 1);

  /// the application splash screen text color.
  static const Color splashTextColor = Color.fromRGBO(247, 247, 247, 1);

  /// the application home screen text color.
  static const Color homeTextColor = Color.fromRGBO(247, 247, 247, 1);

  /// the application home screen background color.
  static const Color homeBackgroundColor = Color.fromRGBO(27, 26, 23, 1);
}
